---
description: 启动或继续一次 PANGEA C/C++ 模块分析
---
# module_analysis

读取 Desktop 已创建 Skill Run 的 `request.md`，按冻结 `SKILL.md` 和 `run_guard.py` 完整执行 Step 01–09。不再创建 Run，不使用 Graph、Action、Worker 或 settle。生命周期只写入 `内部索引/运行状态.json`，分析产物只写 Markdown。
