# 黑盒测试用例

按用例重复以下结构；数量、篇幅由实际行为决定，内容不能只指向内部函数。

## TC-001：外部可验证的行为

关联 FLOW/BRANCH/RISK/EVIDENCE ID；简述本用例要区分的结果，不复制完整风险机理。
**前置条件**：可构造并确认的环境和系统状态。
**操作步骤**：
1. 外部接口、协议、配置或故障注入动作。
**预期结果**：请求结果和后续正常业务的判断标准；不得用被测实现自身作为唯一 Oracle。
**观测方式**：独立的抓包、日志、告警、统计或后续行为。
**清理或恢复**：恢复到可重复执行状态。
