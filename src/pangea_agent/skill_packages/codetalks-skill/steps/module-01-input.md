# 阶段 01：输入与范围

写入 `活文档/输入与范围.md`：目标、支持范围、排除项、真实入口边界、材料冲突和分析限制。记录用户已经选择的范围，不再追问分析重点。
实际消费冻结资产，更新 `内部索引/输入材料索引.json`（无资产时 items=[]，如实说明）；按冻结资产 manifest 允许阶段回查原文。示例用例只表达格式，不作为源码证据。
更新 `内部索引/方法论选择.json` 的 selected/excluded/reason/evidence，始终选中 codetalks-skill；只加载适用方法论。
`内部索引/运行计划.json` 仅维护阶段/流程 ID、工件路径、恢复游标和待复核 ID，不重复存放分析正文。逐流程队列在阶段 02 建立。
阶段 01 合并原范围、材料与方法论工作，不再生成单独的材料消费 Markdown 或覆盖门禁。
