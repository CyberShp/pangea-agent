# 阶段 03：按流程完成分析

## 可直接执行的库接口用例

当被测对象是依赖齐备、可独立执行的公开 C 库接口时，读取 references/executable-case-review.md，在每条用例的操作步骤中直接提供完整 C 程序。初始化、准备、触发、观测、清理和对应数值断言都写在这一个代码块中；正文说明目标、证据和适用边界，预期结果引用代码里的比较，不再重复一套值或末态总述。这样 Reviewer 执行原用例本身，无需重新解释文字中的操作。代码只调用真实实现，不重写业务逻辑。其他对象仍使用受支持的业务入口，无法执行时如实记录限制。

## 返回值与末态：本阶段直接执行

先把每条用例的初态和全部操作列出来，再计算预期。Reviewer 先独立计算，不复制 Producer 的预期或执行表。按每次实际调用，从函数入口逐句到本次 return：引用实际判断的原文，代入本步输入及上一步状态，求出真假；只执行进入分支内的语句。写出本次真正执行的赋值和子调用，以及已经 return 因而没有执行的后续赋值。没有执行的赋值不能改变状态。

通用例子（只说明推演方法，不能复制成项目用例）：
```c
int update(int valid, int *value) {
    if (!valid) return -9;
    *value = 7;
    return 0;
}
```
若调用前 value=2、valid=0，则 !valid 为真，立刻返回 -9，value 仍为2；后面的 value=7 没有执行。错误返回码不能与“设置成功后的状态”混用。若真实代码先赋值再返回错误，则保留已经发生的赋值，不能反向套用“错误一定无副作用”。

用例只保留一条执行序列：步骤 0 初始化 → 编号准备调用 → 主要触发 → 观测 → 清理。步骤 0 写明实际初始化方法和影响断言的初始值；前置条件只列环境、权限、接口和可用夹具，不再存放业务状态。每次调用记录本步返回、变化和保留状态，后一步接着前一步的实际结果执行。循环必须展开核对每轮的准备和触发，不能沿用首轮初态。预期和清理说明引用步骤时点，不另加调用或可选顺序。

清理例子：若 reset 只执行 active=0，调用前 active=1、total=4，则调用后 active=0、total=4；不能写全部归零。若随后为了验证可重用而执行 start，使 active=1，则验证结束时 active=1；如要结束时 active=0，必须在步骤中再执行真实 reset。分别给出主触发后、全部验证后、清理后三个时点的结论；同一状态可以明确引用前一时点。

逐条将文档的返回值和状态断言与这份计算比较，包括“全部”“恢复初始”“没有变化”等总述。发现矛盾时，由原 Producer 修正所有受影响的后续步骤、预期及清理，再由原 Reviewer 从初态重算；审核建议本身也要用同样方法核实。不能用“符合源码”一句话、行号引用或表格齐全代替求值。证据不足保留具体未决项，不给数值正确性 PASS。

设计契约与实现不一致时保留差异，不能为了通过执行而改写契约。只有源码时标注实现行为回归；未执行的推演不能称为实测。更完整说明见 references/return-and-state-proof.md，但以上方法直接在本阶段执行，不依赖另一次文件读取。

读取 references/path-and-case-design.md，按统一的业务路径粒度、耦合关系和独立用例原则执行。

每个流程连续完成源码理解 → 分支/场景推导 → 候选反证 → 风险/SFMEA → 测试设计，再切换下一个流程。不再把全部流程拆成四轮跨步骤写作。

使用 `templates/模块流程模板.md` 作为可裁剪提示，在 `活文档/流程讲解/流程-<FLOW-ID>-<名称>.md` 集中维护：
外部触发与运行时接线、真实主干、重要分支进入条件、状态、资源、数值、并发、异常传播及源码证据。
按复杂度组织正文，不要求固定章节和字数。不适用维度一句说明；复杂的状态机、资源不变量、回绕、竞态和清理路径必须展开，不能因为取消篇幅检查而省略。
每个分支说明外部可构造条件 → 内部判断/保护 → 外部结果，证据使用 repo_id:path:line，区分推导与实际运行。

读取 `references/scenario-expansion-engine.md` 推导适用维度，及 `references/worker-judge-protocol.md` 的候选裁决部分。
候选判断保留完整契约、支持范围、实际传播、保护条件、最小反事实和根因归并。排除、范围限制、补测、合并和待确认保留在分析台账；只有当前实现风险进入风险文档。

`活文档/风险点与SFMEA.md` 是风险唯一主版本：每项保留 risk_id、Flow/Branch/Evidence ID、触发条件、失效模式/原因、局部及系统后果、严重性理由、外部暴露、排除条件和用例 ID。SFMEA 不再另起一套编号或复制源码段落。
`活文档/黑盒测试用例.md` 是用例唯一主版本，使用 `templates/模块用例模板.md`；步骤外部可执行，独立观测和 Oracle 明确，并含清理恢复。内部符号只作追溯；若被测对象本身是公开库接口，可将有证据支持的接口调用作为操作，不能把私有字段注入或不存在的管理命令冒充黑盒入口。风险至少关联一条用例；不可达例外需真实源码依据，正常/边界/补测用例可不关联风险。没有正式风险时如实说明，不凑风险或用例数。

每完成一个流程或形成可用的部分成果：
1. 更新该流程正文、关联风险/用例和同一份分析台账；不重新生成其他流程。
2. 保存运行计划中的当前流程、已确认分支、待查证据、下一步和待复核 ID。
3. 用 progress 报告真实完成数/总数和当前 FLOW；受阻、未决不计为已分析。
4. 更新并 publish-stage 工作台投影。投影是索引与显示摘要，引用正文、稳定 ID 和证据，不成为第二份分析结论。

compact 前保存上述游标并 handoff。恢复先读状态、计划、台账和当前流程，只复查中断的分析中项，不从头重做。

发布业务流程时读取 references/business-flow-projection.md，维护 mainline_steps 和 branches，供专用阅读器展示主干及分支。
